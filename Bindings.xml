<Bindings>
  <!-- TurtleControl Custom Buttons -->
  <Binding name="TURTLECONTROL0" runOnUp="true" header="TURTLECONTROL">TurtleControlButton(0)</Binding>
  
  <!-- UP: Inventory -->
  <Binding name="TURTLECONTROL1" runOnUp="true">
    if ( keystate == "down" ) then
      ToggleCharacter("PaperDollFrame")
      ToggleKeyRing()
      OpenAllBags()
    end
  </Binding>
  
  <!-- RIGHT: QuestLog -->
  <Binding name="TURTLECONTROL2" runOnUp="true">
    if ( keystate == "down" ) then
      ToggleQuestLog()
    end
  </Binding>
  
  <!-- DOWN: Talents & Social -->
  <Binding name="TURTLECONTROL3" runOnUp="true">
    if ( keystate == "down" ) then
      ToggleTalentFrame()
      ToggleFriendsFrame()
    end
  </Binding>
  
  <!-- LEFT: Spellbook -->
  <Binding name="TURTLECONTROL4" runOnUp="true">
    if ( keystate == "down" ) then
      ToggleSpellBook(BOOKTYPE_SPELL)
    end
  </Binding>
</Bindings>
